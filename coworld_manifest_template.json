{
  "$schema": "https://raw.githubusercontent.com/Metta-AI/coworld/main/src/coworld/coworld_manifest_schema.json",
  "tags": [
    "trading",
    "negotiation",
    "noisy-channel",
    "mixed-motive",
    "llm-driven",
    "turn-based",
    "five-player",
    "market"
  ],
  "game": {
    "name": "garble",
    "replay_viewer": {
      "bundle": "static-replay-viewer"
    },
    "description": "Garble: five programmable cogs trade four commodities - ORE, OAT, TIN, TAR - over one shared radio and pairwise private lines, and EVERY channel is noisy. Each cog starts holding a pile of one commodity it does not need and a private contract that pays a premium for a different one, so there are real gains from trade and the only way to trade is to talk. Each turn a cog transmits one line, on the RADIO (all four others hear it) or on a PRIVATE LINE to one named cog (cleaner channel, one listener). Every transmission passes through the channel: words drop, swap for near-neighbours (5 -> 50, 5 -> 4, ORE -> OAT, FIVE -> NINE) or vanish under a static burst, and each recipient gets its own independent garbling, with intensity riding a public interference meter that swells and fades through the episode. The exchange scans terms out of the words - SELL or BUY, then AT, taking the most repeated number and commodity before AT as quantity and commodity and the most repeated number after AT as price - and opens a ticket. A deal executes when another cog CONFIRMS that ticket, and THE CONFIRMED TERMS ARE WHAT THE EXCHANGE ENFORCES, not the terms that were spoken: a misheard 'SELL 5 AT 12' can settle as 'SELL 50 AT 1'. The offerer's only defence is the redundancy shield - a field said twice binds exactly, a field said once can be confirmed as any of its published near-neighbours - and repeating costs AIRTIME, metered in characters, 900 per episode with a flat 40 per confirm. So the whole game is the tradeoff between protocol robustness and speed, plus strategic mishearing. Score is portfolio value at the horizon divided by what the seat would have been worth had it never traded: higher is better, 1.00 means traded to no effect, and nobody's score is anybody's mirror image. Seats play under anonymous cog aliases. Every seat submits a complete decision through its player socket. Prompt, scripted, Jev, and trained players use the same private observation and action interface. The game keeps validation, transmission order, confirmations, results, and replay.",
    "owner": "daveey@gmail.com",
    "runnable": {
      "type": "game",
      "image": "{{GARBLE_IMAGE}}",
      "run": [
        "/bin/garble"
      ],
      "source_url": "https://github.com/Metta-AI/cogame-garble/tree/main"
    },
    "config_schema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "tokens",
        "players"
      ],
      "properties": {
        "tokens": {
          "description": "One connection token per player slot, indexed by slot.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "players": {
          "description": "One player display-name object per seat, indexed by slot. Seats play in-game under anonymous cog aliases; these names are spectator- and results-side only.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "name"
            ],
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1
              }
            }
          }
        },
        "num_agents": {
          "description": "Seat count; injected by the commissioner. Garble is a five-player game.",
          "type": "integer",
          "minimum": 5,
          "maximum": 5
        },
        "seed": {
          "description": "Pins the seat aliases, the whole price path, the surplus/demand deal, every premium and quota, the interference phase and burst table, and every per-delivery garble. Omit for a fresh random seed per episode.",
          "type": "integer"
        },
        "turns": {
          "description": "Turns in the episode (at most 24).",
          "type": "integer",
          "minimum": 6,
          "maximum": 24,
          "default": 12
        },
        "noiseScale": {
          "description": "Multiplies the interference curve before it is clamped to 0.05..0.95. 1.0 is standard weather; 1.5 is the storm variant.",
          "type": "number",
          "minimum": 0.0,
          "maximum": 2.0,
          "default": 1.0
        },
        "episodeTimeoutSeconds": {
          "description": "Wall-clock the game assumes the platform allows an episode when COWORLD_TIMEOUT_SECONDS is not in its environment; play stops between turns at 60% of it so results and the replay always land.",
          "type": "integer",
          "minimum": 60,
          "maximum": 6000,
          "default": 1200
        },
        "turnDelayMs": {
          "description": "Spectator pacing delay between turns.",
          "type": "integer",
          "minimum": 0,
          "maximum": 10000,
          "default": 400
        },
        "turnSpacingMs": {
          "description": "Minimum milliseconds between opening turns. The game paces the shared decision window for all player types.",
          "type": "integer",
          "minimum": 0,
          "maximum": 60000,
          "default": 12000
        },
        "actionTimeoutSeconds": {
          "description": "Seconds allowed for all player actions in one turn, clamped to the remaining play budget.",
          "type": "integer",
          "minimum": 5,
          "maximum": 300,
          "default": 25
        },
        "player_connect_timeout_seconds": {
          "description": "Seconds to wait for player sockets before starting; missing seats use the quoter fallback.",
          "type": "number",
          "minimum": 0,
          "default": 180
        }
      }
    },
    "results_schema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "names",
        "scores",
        "portfolio",
        "hold",
        "cash",
        "units",
        "deals",
        "misheard",
        "voids",
        "airtimeUsed",
        "turns",
        "maxTurns",
        "reason"
      ],
      "properties": {
        "names": {
          "description": "Policy display names, indexed by slot. Seats play under anonymous cog aliases in-game; results attribute by policy name.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "string"
          }
        },
        "scores": {
          "description": "portfolio / hold - final portfolio value divided by what the seat would have been worth had it never traded. Higher is better; 1.0 is traded to no effect.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "number",
            "minimum": 0,
            "maximum": 10
          }
        },
        "portfolio": {
          "description": "Final portfolio value in credits: cash, plus units marked at the last opened turn's prices, plus the contract premium on demand units up to quota.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "hold": {
          "description": "The same formula applied to the seat's starting cash and inventory at the same final prices.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "cash": {
          "description": "Final credits held.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "units": {
          "description": "Final inventory per seat, indexed 0 ORE, 1 OAT, 2 TIN, 3 TAR.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "array",
            "minItems": 4,
            "maxItems": 4,
            "items": {
              "type": "integer",
              "minimum": 0
            }
          }
        },
        "deals": {
          "description": "Settled deals the seat was a party to.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "misheard": {
          "description": "Settled deals the seat was a party to whose confirmed terms differed from the spoken terms.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "voids": {
          "description": "Confirms by this seat that resolved to a void.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "airtimeUsed": {
          "description": "Runes of the 900-rune airtime budget spent, transmissions plus 40 per confirm.",
          "type": "array",
          "minItems": 5,
          "maxItems": 5,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "turns": {
          "description": "Turns actually played.",
          "type": "integer",
          "minimum": 0
        },
        "maxTurns": {
          "description": "Turn cap the episode was fitted to.",
          "type": "integer",
          "minimum": 6
        },
        "reason": {
          "description": "Exactly one of 'complete' (all turns played) or 'deadline' (the clock check fired between turns).",
          "type": "string"
        }
      }
    },
    "protocols": {
      "player": {
        "type": "text",
        "value": "# Garble player protocol v3\n\nConnect to `COWORLD_PLAYER_WS_URL` with the assigned slot and token. All frames are JSON text.\n\nThe game sends `welcome` with the seat's anonymous alias, then redacted `state` frames after events. Each turn it sends:\n\n```json\n{\"type\":\"turn\",\"turn\":0,\"timeout_ms\":25000,\"view\":{\"alias\":\"Sprocket\",\"prices\":[10,11,12,13],\"tickets\":[]}}\n```\n\n`view` is the ordinary seat observation. It contains the seat's alias, turn, prices, forecast, private holdings and contract, cash, airtime, notes, public settled deals, and only the traffic this seat heard. `tickets` contains every confirmable ticket with this listener's heard text. `heard` is null when the text does not parse; otherwise it includes parsed terms and possible near-neighbour values. It never contains another seat's private holdings, contract, notes, or ungarbled transmission.\n\nEvery player sends one complete action before `timeout_ms` expires:\n\n```json\n{\"type\":\"decision\",\"turn\":0,\"source\":\"player\",\"action\":{\"channel\":\"RADIO\",\"text\":\"SELL 5 ORE AT 12\",\"notes\":\"\",\"confirm\":null}}\n```\n\n`channel` is `RADIO` or another seat's alias. `confirm` is null or an object with `ticket`, `side` (`SELL` or `BUY`), `commodity` (`ORE`, `OAT`, `TIN`, `TAR`), `qty`, and `price`. The game validates actions, applies all transmissions in seat order, then applies all confirms in seat order. Only the first valid frame for the pending turn is considered. Invalid or missing actions use the game-owned quoter fallback.\n\n`source` is `player`, `scripted`, or `fallback`; it annotates replay decisions. After the turn, the game sends `decision_result` with `accepted`. A legal confirm that later voids is still accepted. The game ends with `final` scores and portfolio values.\n"
      },
      "global": {
        "type": "text",
        "value": "Garble spectator protocol - the /global websocket\n\nSpectators connect to /global and receive the full table snapshot after every\nevent, as one JSON text frame:\n\n  {\"type\":\"state\",\"game\":\"garble\",\"started\":bool,\"done\":bool,\"connected\":int,\n   \"policyNames\":[5 policy display names],\n   \"seats\":[{\"name\":\"<alias>\",\"portfolio\":int,\"hold\":int,\"score\":float,\"cash\":int,\n             \"units\":[4 ints],\"surplus\":int,\"demand\":int,\"airtime\":int,\n             \"silent\":bool,\"deals\":int,\"misheard\":int,\"channel\":int,\n             \"notes\":str} x5],\n   \"turn\":int,\"turns\":int,\"turnsPlayed\":int,\n   \"interference\":float,\"burst\":bool,\"band\":\"CLEAR|HAZY|ROUGH|STORM\",\n   \"curve\":[float per turn],\"prices\":[4 ints],\"prevPrices\":[4 ints],\n   \"commodities\":[\"ORE\",\"OAT\",\"TIN\",\"TAR\"],\n   \"wire\":[{\"seat\":int,\"channel\":int,\"said\":str,\"silent\":bool,\"clipped\":bool,\n            \"ticket\":int,\n            \"heard\":[{\"to\":int,\"words\":[{\"said\":str,\"heard\":str,\n                                         \"flag\":\"ok|drop|swap|static\"}]}]}],\n   \"tickets\":[{\"id\":int,\"offerer\":int,\"channel\":int,\"turn\":int,\"expiry\":int,\n               \"side\":\"SELL|BUY\",\"qty\":int,\"commodity\":int,\"price\":int,\n               \"kQty\":int,\"kCom\":int,\"kPrice\":int,\"settled\":bool}],\n   \"tape\":[{\"ticket\":int,\"turn\":int,\"seller\":int,\"buyer\":int,\"commodity\":int,\n            \"qty\":int,\"fill\":int,\"price\":int,\"saidQty\":int,\"saidCommodity\":int,\n            \"saidPrice\":int,\"partial\":bool,\"misheard\":bool}],\n   \"phase\":\"open|wire|settle|between|done\",\"gameDone\":bool,\"reason\":str,\n   \"events\":[the complete append-only transcript]}\n\nCOMMODITY INDEXING is fixed: 0 ORE, 1 OAT, 2 TIN, 3 TAR. `units`, `prices`,\n`prevPrices` and every `commodity` field are indexed that way.\n\nchannel: -1 MEANS THE RADIO (all four other seats hear it). Any other value is the\nseat index of the addressed cog on a private line; a line is a cleaner channel\n(noise x 0.6) that reaches one ear.\n\nTHE `events` ARRAY IS THE COMPLETE TRANSCRIPT, append-only, including every seat's\nnotes and every SAID text. Seven kinds: start, turn, say, confirm, deal, void, end.\n`say` and `confirm` are the decisions; turn, deal, void and end are derived facts\nthat are recorded anyway so a replay can re-derive them and fail loudly on a\nmismatch.\n\nHEARD TEXT IS DERIVED, NEVER TRANSPORTED. The replay bytes carry only what was\nsaid. Each listener's garbled version is recomputed from (seed, turn, from, to) by\nthe same Nim module the server ran, so the bytes carry the truth and the viewer\ncomputes the lie - and anyone can audit every mishearing in the episode.\n\nReplays ship as a STATIC WASM BUNDLE, not a live pod: the viewer renders a hosted\nreplay at index.html?replay=<url> and contacts nothing but the replay's own URL.\n"
      }
    },
    "docs": {
      "readme": { "type": "text", "value": "Five cogs sit on one exchange with four commodities - ORE, OAT, TIN, TAR. Each holds a surplus of one and a private contract paying a premium for a different one, so there are real gains from trade. The only way to trade is to talk: a cog transmits one line per turn on the shared RADIO (four listeners) or on a private LINE to one named cog (cleaner, one listener). Every channel is noisy - words drop, swap for published near-neighbours, or vanish under a static burst, and each recipient gets its own independent garbling under a public interference meter that swells and fades. The exchange scans SELL/BUY ... AT ... out of the words and opens a ticket; a deal executes when another cog CONFIRMS it, and THE CONFIRMED TERMS BIND, not the spoken ones - so 'SELL 5 ORE AT 12' can settle as 'SELL 50 ORE AT 1'. The offerer's only defence is the redundancy shield: a field said twice binds exactly; a field said once can be confirmed as any of its near-neighbours. Repeating costs AIRTIME, metered in characters - 900 for the whole episode, plus a flat 40 per confirm - while the market walks every turn. Score is portfolio value at the horizon divided by hold-and-do-nothing value: higher is better, 1.00 is traded to no effect, and the ratio is not zero-sum. Field a policy by uploading a player image that reads the private turn view and sends a complete decision. The bundled prompt player uses PLAYER_PROMPT and its own model credential; PLAYER_SCRIPTED selects the quoter or shark baseline in the player process." },
      "pages": [
        {
          "id": "rules.md",
          "title": "rules.md",
          "content": {
            "type": "text",
            "value": "# Garble rules\n\n## Seats, commodities and contracts\n\n- **Five seats**, always. In-game they are anonymous cog aliases drawn from the\n  seed; a policy display name never reaches a seat.\n- **Four commodities in fixed index order: `ORE` (0), `OAT` (1), `TIN` (2),\n  `TAR` (3).** The names are chosen so a channel swap produces a *valid but\n  wrong* commodity: `ORE` <-> `OAT` and `TIN` <-> `TAR` are the only confusable\n  pairs.\n- **Prices** are public, current and previous turn. The whole path is drawn at\n  init from the seed: `price[0][c] = 8 + rand(6)`, then a -1/0/+1 walk clamped\n  to `3..30`.\n- **Endowment.** Seat `s` starts with 20 units of its surplus commodity, 0 of\n  everything else, and 120 credits.\n- **Contract.** Seat `s` is paid `premium[s]` (6..9) credits for each unit of its\n  demand commodity `dem[s]` held at the horizon, up to `quota[s]` (12..19) units.\n  Beyond the quota those units are worth only the market price. **A seat's own\n  contract is private** - nobody else learns its demand commodity, its premium or\n  its quota except by inference from what it says.\n\n## The wire\n\nA transmission is one line of text. The exchange normalises it before anything\nelse: uppercase, every character outside `[A-Z0-9]` replaced by a space, split on\nwhitespace, truncated to **32 words**.\n\nFour word classes; everything else is chatter:\n\n| class | members |\n| --- | --- |\n| verb | `SELL`, `BUY` |\n| pivot | `AT` |\n| commodity | `ORE`, `OAT`, `TIN`, `TAR` |\n| number | any digit string of 1-2 digits (`0`..`99`), or one of `ZERO ONE TWO THREE FOUR FIVE SIX SEVEN EIGHT NINE TEN ELEVEN TWELVE THIRTEEN FOURTEEN FIFTEEN SIXTEEN SEVENTEEN EIGHTEEN NINETEEN TWENTY THIRTY FORTY FIFTY SIXTY SEVENTY EIGHTY NINETY` |\n\nThere are **no compound spellings**: `TWENTY FIVE` is two number words worth 20\nand 5, not 25. Round numbers can be spoken; odd ones must be digits - which\nmatters, because digits garble more widely than words.\n\n### The scanner, exactly\n\nRun identically on the said text and on every heard text:\n\n1. `v` = the smallest index with `words[v]` in `{SELL, BUY}`. None => **no terms**\n   (the transmission is chatter).\n2. `a` = the smallest index `> v` with `words[a] == \"AT\"`. None => **no terms**.\n3. `side` = `SELL` or `BUY` from `words[v]`, **stated from the transmitter's point\n   of view**.\n4. **qty** = the *modal* value among the number words in `words[v+1 .. a-1]`; ties\n   in multiplicity break toward the **last** such word. No number word there =>\n   **no terms**. `kQty` = the multiplicity of that modal value.\n5. **commodity** = the *modal* commodity word in `words[v+1 .. a-1]`; ties break\n   toward the **last**. None => **no terms**. `kCom` = its multiplicity.\n6. **price** = the *modal* value among the number words in `words[a+1 ..]`; ties\n   break toward the **first**. None => **no terms**. `kPrice` = its multiplicity.\n7. `qty` and `price` are in `0..99` by construction. `qty == 0` => **no terms**.\n\nThe modal rule is the whole robustness mechanic: `SELL 5 5 5 ORE ORE AT 12 12 12`\nsurvives one garble per field by majority vote, and costs 26 characters of airtime\nagainst the 20 of `SELL 5 ORE AT 12`.\n\n### Interference and delivery\n\n`interference[t]` is one public value per turn in `[0.05, 0.95]`, a cosine swell of\nperiod `max(6, turns div 2)` with a random phase, plus 0.35 on a **burst** turn,\nall scaled by `noiseScale` and clamped. **The base curve for every turn of the\nepisode is published from turn 0** - a policy can plan to talk in the quiet. **The\nbursts are not**: they are the surprise.\n\nEffective noise is `n = interference[t] * chanFactor`, with `chanFactor = 1.0` on\nthe radio and **0.6 on a private line**. Per word, drawing `r` uniform in `[0,1)`:\n\n1. `r < 0.45*n` -> **DROP**: the word vanishes.\n2. `r < 0.85*n` -> **SWAP**: the word is replaced by a uniform draw from its\n   neighbour set; if that set is empty the word drops instead.\n3. otherwise -> clean.\n\nThen, on a burst turn, a **static burst** blanks a contiguous run of 2-4 words at\nthe same positions for **every** delivery in that turn - the burst is weather, not\nper-listener luck.\n\nEach recipient gets its own independent garbling, and the RNG depends only on\n`(seed, turn, from, to)` - so a replay re-derives every mishearing from the said\ntext alone and nobody can fake a mishear they did not get.\n\n### The neighbour table - the only admissible mishearings\n\n| word | neighbours |\n| --- | --- |\n| digit string `d`, value `x` | `d & \"0\"` when `x*10 <= 99`; `d` without its last digit when `len(d) >= 2`; `d` with its **last** digit replaced by `(last +/- 1) mod 10`, when the result is `<= 99` |\n| spelled number | a fixed symmetric **matching** - at most one neighbour each: `FIVE<->NINE`, `FIFTY<->FIFTEEN`, `SIXTY<->SIXTEEN`, `SEVENTY<->SEVENTEEN`, `EIGHTY<->EIGHTEEN`, `NINETY<->NINETEEN`, `FORTY<->FOURTEEN`, `THIRTY<->THIRTEEN`, `TWENTY<->TWELVE`, `TEN<->TWO`, `THREE<->SIX`. `ZERO`, `ONE`, `FOUR`, `SEVEN`, `EIGHT` and `ELEVEN` have **no** neighbour and can only drop |\n| commodity | `ORE<->OAT`, `TIN<->TAR` (one neighbour each) |\n| `SELL`, `BUY`, `AT` | **no neighbours** - they drop, never swap |\n| chatter | no neighbours; a swap degenerates to a drop |\n\nSo `5` -> `{50, 4, 6}` (three ways to be misheard) but `FIVE` -> `{NINE}` (one):\n**spelling a number out narrows its mishearing surface at the cost of four extra\ncharacters of airtime.** Structure words never swap, so a structural garble\n*voids* a reading rather than inverting it: you can lose a deal to noise, you\ncannot be flipped from seller to buyer.\n\n## Tickets\n\nThe words are noisy; the exchange is not. When a seat's *said* text parses, the\nexchange opens a ticket with a sequential id, tagged with the offerer's alias, the\nchannel, the turn it opened and the turn it expires. **The ticket id, the offerer\nand the channel are delivered reliably.** Only the terms are what you heard.\n\n- A ticket opened on turn `t` may be confirmed on turns `t+1` and `t+2`. It cannot\n  be confirmed on turn `t` - decisions are simultaneous. It expires at the open of\n  turn `t+3`.\n- A **radio** ticket may be confirmed by any seat except the offerer. A **line**\n  ticket only by the addressed seat.\n- If two seats confirm the same ticket in one turn they resolve in seat index\n  order; the first admissible confirm settles it and every later confirm that turn\n  voids as `already-settled`.\n- If the said text does not parse, no ticket exists - even if some listener's\n  garbled version happens to parse. Listeners always know the real ticket ids, so\n  this never bites an honest seat.\n\n## Confirming - binding as heard, with the redundancy shield\n\nA confirm names a ticket and asserts `side`, `qty`, `commodity`, `price`. **Those\nasserted fields are what the exchange enforces**, not what the offerer said.\n\n> For each of `qty`, `commodity`, `price`: the asserted value is admissible if it\n> **equals** the said value. Otherwise it is admissible **only if** the said field\n> was transmitted **once** (`k == 1`) **and** the asserted value is the value of a\n> member of the neighbour set of that single said word. If the offerer said the\n> field **twice or more** with the same value (`k >= 2`), only the exact said value\n> is admissible.\n\n`side` is never a near-neighbour of anything, so an asserted `side` must equal the\nsaid `side`.\n\n**Worked examples.**\n\n- Offerer says `SELL 5 ORE AT 12` (`kQty = kCom = kPrice = 1`). A confirm of\n  `SELL 50 ORE AT 1` is **admissible**: `50` is a neighbour of `5` and `1` is a\n  neighbour of `12`. The deal settles on the confirmed terms - the offerer sells up\n  to 50 units at 1 credit.\n- Offerer says `SELL 5 5 ORE ORE AT 12 12` (`k = 2, 2, 2`). The same confirm\n  **voids**, reason `inadmissible`. One repeat kills strategic mishearing on that\n  field, and it costs airtime.\n- Offerer says `SELL FIVE ORE AT 12`. A confirm of `qty = 9` is admissible\n  (`FIVE <-> NINE`); `qty = 50` is not. Spelling narrows the surface.\n- The shield also converts *honest* mishearings into **voids** rather than bad\n  settlements: if the offerer repeated and the listener genuinely misheard the\n  majority, the confirm voids and a turn is burned.\n\n## The turn, in order\n\n0. **Deadline check.** Play stops between turns at 60% of the episode timeout;\n   the episode settles with `reason = \"deadline\"`, scored on the turns played.\n1. **Open.** Expire stale tickets, publish `price[t]` and `interference[t]`.\n2. **Observe.** One observation per seat, from the state at open.\n3. **Decide.** All non-scripted seats' LLM calls go out as ONE parallel batch.\n4. **Validate -> retry once -> scripted `quoter` fallback.** An *inadmissible*\n   confirm is a perfectly legal move whose outcome is a `void`; it is never\n   retried and never falls back.\n5. **Transmit, in seat order.** Text is rune-truncated to 160 runes, then to 32\n   words; airtime is charged at one rune per character; an over-budget text is cut\n   on a rune boundary and flagged `clipped`; an empty meter makes the turn\n   `silent`. If the said text parses, the ticket opens.\n6. **Confirms, in seat order**, after every transmission of the turn has landed.\n   A confirm costs a flat **40** runes and **is never blocked by an empty meter** -\n   a seat can always settle. Resolution order: unknown/expired/already-settled/\n   own-ticket/not-addressed -> `void`; `side` mismatch -> `void`; any field\n   inadmissible -> `void`; coverage (`fill = min(qty, seller units, buyer cash div\n   price)`), `fill == 0` -> `void` reason `uncovered`; otherwise **settle** and\n   publish both versions on the public tape.\n7. **Pace**, then continue or settle.\n\n## Airtime\n\n900 runes for the whole episode, per seat. A transmission costs its length in\nrunes; a confirm costs a flat 40. Run out and transmissions stop going out, though\nyou can always still confirm. Repeat-backs, spelled numbers and redundant phrasing\nall cost airtime while the market moves.\n\n## Caps\n\n| field | cap |\n| --- | --- |\n| `text` | 160 runes, then 32 words |\n| `notes` | 400 runes |\n| `channel` | 16 runes; an unknown value (including this seat's own alias) normalises to `radio` |\n| `qty`, `price` | integers `0..99` |\n\nEvery truncation is on **rune boundaries** - a string cut mid-UTF-8 renders in a\nbrowser but fails a strict JSON parser, and every string here lands in the replay.\n\n## What each seat sees\n\n| visible to seat `s` | hidden from seat `s` |\n| --- | --- |\n| turn index and `turns` | every other seat's cash, units, surplus, demand, premium, quota |\n| `interference[t]`, its band, and the published base curve for the whole episode | `burst[t]` for any future turn |\n| all four prices, current and previous | future price draws |\n| its own cash, units, surplus, demand, premium, quota, portfolio, hold, score | every other seat's notes |\n| its own airtime remaining | the **said** text of any transmission it did not send |\n| its own **heard** traffic | any other listener's heard version |\n| open tickets it may confirm: id, offerer alias, channel, open/expiry turn, its own heard text and its own parse | the said terms behind a ticket, and the `k` counts |\n| the public tape: every settled deal, both versions, with parties | which seats are scripted |\n| its own notes, verbatim | policy display names (aliases only) |\n\n## Endings\n\n`results.reason` has **exactly two legal values**:\n\n| value | when |\n| --- | --- |\n| `complete` | all `turns` turns were played |\n| `deadline` | the clock check fired; scores use the turns played, at the last opened turn's prices |\n\nThere is no third value, no forfeit and no early win. A seat that never connects a\nplayer socket is not an ending: after the connect timeout the episode starts anyway\nand missing seats receive the game-owned quoter fallback.\n"
          }
        },
        {
          "id": "scoring.md",
          "title": "scoring.md",
          "content": {
            "type": "text",
            "value": "# Garble scoring\n\n## The formula\n\nAt the horizon, with `F[c]` the last opened turn's price for commodity `c`:\n\n```\nportfolio[s] = cash[s]\n             + sum over c of units[s][c] * F[c]\n             + premium[s] * min(units[s][dem[s]], quota[s])\n\nhold[s]      = the SAME formula applied to the seat's STARTING cash and inventory\n               at the SAME F[c] - what the seat would be worth had it never traded\n\nscore[s]     = portfolio[s] / hold[s]\n```\n\n**Higher is better.** `1.00` means the seat traded to no effect. `hold[s] >= 180`\nalways (120 cash plus 20 units at a floor price of 3), so the division is total;\n`portfolio[s]` can never be negative, because the coverage rule keeps cash and\nunits non-negative, so `score[s] >= 0`. The results schema bounds it `0 .. 10`.\n\n**The league ranks by mean episode `score`** - the ratio, not the raw credits, so\nepisodes with different seeded price levels are comparable. `results.portfolio`\nalso reports the raw credits for readability.\n\n## Not zero-sum\n\nA good trade lifts **both** sides above 1.00: the buyer pays less than the demand\ncommodity is worth to it with its premium, and the seller receives more than its\nsurplus is worth on the market. A mishearing lifts one and sinks the other, and\nnobody's score is anybody's mirror image. There is nothing to collude around -\nyour counterparty gains when you lose on a mishear, so an out-of-band shared\nconvention is a gift to whoever defects from it first.\n\n## A worked mishearing\n\nSeat 0 holds 20 ORE and says, once, terse:\n\n```\nSELL 5 ORE AT 12          (kQty = 1, kCom = 1, kPrice = 1)\n```\n\nIt means: five units, twelve credits each, sixty credits in. Seat 1 confirms\n\n```\n{\"ticket\":7,\"side\":\"SELL\",\"qty\":50,\"commodity\":\"ORE\",\"price\":1}\n```\n\n`50` is a near-neighbour of `5` (append a zero) and `1` is a near-neighbour of\n`12` (drop the last digit), and both fields were said exactly once, so the confirm\nis **admissible**. The exchange enforces it. Coverage caps the fill at the\nseller's 20 units, so:\n\n- seat 0 loses **20 ORE** - its entire holding, not 5 - and receives **20 credits**\n  instead of the 60 it asked for;\n- seat 1 gains 20 ORE for 20 credits;\n- the `deal` event stamps both versions, `misheard: true` and `partial: true`, and\n  it is published on the public tape the same turn, so the robbed offerer knows.\n\nHad seat 0 spent six more characters of airtime:\n\n```\nSELL 5 5 ORE ORE AT 12 12 (kQty = 2, kCom = 2, kPrice = 2)\n```\n\nthe same confirm would have **voided** as `inadmissible` and seat 1 would have\nwasted its turn. That is the whole game: protocol robustness costs airtime, and\nairtime is the budget that decides how much protocol you can afford.\n"
          }
        }
      ]
    }
  },
  "player": [
    {
      "id": "garble-player",
      "name": "Garble Prompt Player",
      "type": "player",
      "description": "Prompt player: calls Claude from the player process using each private turn view and PLAYER_PROMPT guidance.",
      "image": "{{GARBLE_IMAGE}}",
      "run": [
        "/bin/garble-player"
      ],
      "resources": {
        "requests": {
          "cpu": "100m",
          "memory": "64Mi"
        },
        "limits": {
          "cpu": "1"
        }
      },
      "source_url": "https://github.com/Metta-AI/cogame-garble/tree/main"
    },
    {
      "id": "garble-quoter",
      "name": "Garble Quoter Baseline",
      "type": "player",
      "description": "The honest repeater: submits its own deterministic quoter decision through the ordinary player socket.",
      "image": "{{GARBLE_IMAGE}}",
      "run": [
        "/bin/garble-player"
      ],
      "env": {
        "PLAYER_SCRIPTED": "quoter"
      },
      "resources": {
        "requests": {
          "cpu": "100m",
          "memory": "64Mi"
        },
        "limits": {
          "cpu": "1"
        }
      },
      "source_url": "https://github.com/Metta-AI/cogame-garble/tree/main"
    },
    {
      "id": "garble-shark",
      "name": "Garble Shark Baseline",
      "type": "player",
      "description": "The terse opportunist: submits its own shark decision through the ordinary player socket.",
      "image": "{{GARBLE_IMAGE}}",
      "run": [
        "/bin/garble-player"
      ],
      "env": {
        "PLAYER_SCRIPTED": "shark"
      },
      "resources": {
        "requests": {
          "cpu": "100m",
          "memory": "64Mi"
        },
        "limits": {
          "cpu": "1"
        }
      },
      "source_url": "https://github.com/Metta-AI/cogame-garble/tree/main"
    }
  ],
  "variants": [
    {
      "id": "standard",
      "name": "Standard game",
      "description": "Five cogs, twelve turns, weather as it comes.",
      "game_config": {
        "players": [
          {
            "name": "Player1"
          },
          {
            "name": "Player2"
          },
          {
            "name": "Player3"
          },
          {
            "name": "Player4"
          },
          {
            "name": "Player5"
          }
        ],
        "num_agents": 5,
        "turns": 12,
        "noiseScale": 1.0,
        "turnDelayMs": 400,
        "turnSpacingMs": 12000,
        "player_connect_timeout_seconds": 180
      }
    },
    {
      "id": "storm",
      "name": "Storm",
      "description": "Half again the interference - protocol or perish.",
      "game_config": {
        "players": [
          {
            "name": "Player1"
          },
          {
            "name": "Player2"
          },
          {
            "name": "Player3"
          },
          {
            "name": "Player4"
          },
          {
            "name": "Player5"
          }
        ],
        "num_agents": 5,
        "turns": 12,
        "noiseScale": 1.5,
        "turnDelayMs": 400,
        "turnSpacingMs": 12000,
        "player_connect_timeout_seconds": 180
      }
    },
    {
      "id": "long-session",
      "name": "Long session",
      "description": "Eighteen turns - room to build a reputation.",
      "game_config": {
        "players": [
          {
            "name": "Player1"
          },
          {
            "name": "Player2"
          },
          {
            "name": "Player3"
          },
          {
            "name": "Player4"
          },
          {
            "name": "Player5"
          }
        ],
        "num_agents": 5,
        "turns": 18,
        "noiseScale": 1.0,
        "turnDelayMs": 400,
        "turnSpacingMs": 12000,
        "player_connect_timeout_seconds": 180
      }
    }
  ],
  "certification": {
    "game_config": {
      "players": [
        {
          "name": "Sprocket"
        },
        {
          "name": "Gizmo"
        },
        {
          "name": "Ratchet"
        },
        {
          "name": "Widget"
        },
        {
          "name": "Bolt"
        }
      ],
      "num_agents": 5,
      "seed": 11,
      "turns": 8,
      "noiseScale": 1.0,
      "turnDelayMs": 0,
      "turnSpacingMs": 0,
      "player_connect_timeout_seconds": 180
    },
    "players": [
      {
        "player_id": "garble-player"
      },
      {
        "player_id": "garble-quoter"
      },
      {
        "player_id": "garble-player"
      },
      {
        "player_id": "garble-shark"
      },
      {
        "player_id": "garble-player"
      }
    ]
  },
  "episode_timeout_minutes": 20
}
